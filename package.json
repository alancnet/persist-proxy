{
  "name": "persist-proxy",
  "version": "0.1.9",
  "description": "A two-part port-forwarder that can persist a connection through a network change or outage.",
  "main": "src/index.js",
  "bin": {
    "persist-proxy": "bin/persist-proxy"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alan Colon",
  "license": "ISC",
  "dependencies": {
    "alco": "0.0.3",
    "rx": "^4.1.0",
    "serial-stream": "0.0.2",
    "uuid": "^3.0.0",
    "yargs-parser": "^4.0.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
